# Dev: FastAPI address (must match backend API_HOST / API_PORT in repo .env)
VITE_DEV_PROXY_TARGET=http://127.0.0.1:8000

# Production build only: public API origin, no trailing slash (leave unset for same-origin)
# VITE_API_BASE_URL=https://your-api.example.com
